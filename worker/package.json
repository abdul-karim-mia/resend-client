{
  "name": "worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "db:apply": "wrangler d1 execute resend-client-db --local --file=./schema.sql",
    "db:apply:remote": "wrangler d1 execute resend-client-db --remote --file=./schema.sql"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.8.0",
    "hono": "^4.12.0",
    "nanoid": "^5.1.0",
    "resend": "^6.12.0",
    "zod": "^4.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.21",
    "typescript": "^6.0.0",
    "wrangler": "^4.101.0"
  }
}
